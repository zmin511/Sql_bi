# SQL BI

Portable single-file SQL query builder for 1C/SQL metadata exported as XLSX or MXL.

## Usage

Open `index.html` or `sql_bi_v9.html` in a browser, load an `.xlsx` or `.mxl` metadata file, select fields in the tree, and copy the generated SQL.

## Version

Current published version: v9.
